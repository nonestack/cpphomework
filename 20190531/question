1. 算法库中有哪些类型的操作？什么是函数对象？
	查询，排序，计数，和一些其他操作

	一个类将()运算符重载后，这个类就称为函数对象类，这个类的对象就是函数对象
	函数对象也是一个对象，但使用形式看起来就像函数调用


2. 容器、迭代器、算法之间的关系是怎样的？他们是如何结合在一起的？
	一个容器就是一个特定类型的集合
	迭代器将容器和算法结合在一起，使得容器可以调用算法

3. 什么是迭代器失效问题？该问题是如何产生的？怎样避免产生迭代器失效问题？
	当迭代器执行增删改时，部分迭代器会失效
	当容器的vector进行动态扩容的时候，之前的迭代器都会失效

	通过尽量少使用增删改的操作，在初始时给容器足够大的容量
   
4. 什么是回调函数，注册回调函数，执行回调函数？
	回调函数是通过函数指针调用的函数
	为了让使用模块知道回调函数，需要对回调函数进行注册
	当回调函数所需要的条件得到满足时，就执行回调函数
